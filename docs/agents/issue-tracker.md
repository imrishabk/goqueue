# Issue Tracker

- **Type:** Local markdown (solo project, no `gh` auth required)
- **Location:** `.scratch/<feature>/` — each feature is a folder, each issue is `spec.md` or `NNN-*.md`
- **Spec for this feature:** `.scratch/goqueue-mvp/spec.md`
- **Workflow:** `to-spec` writes `spec.md`, `to-tickets` splits into `issues/*.md` with blocking edges, `implement` picks one issue
- **Remote:** `git@github.com:imrishabk/goqueue.git` (GitHub exists but not used for issues in MVP; flip to GitHub by running `/setup-matt-pocock-skills` and changing this file)
- **PRs as request surface:** off
